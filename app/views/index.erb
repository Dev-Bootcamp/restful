<div class="container">
  <h1>Welcome to Notes</h1>

  <table>
    <tr><th>Notes</th><th>Written by</th></tr>
    <% Note.all.each do |note| %>
      <tr><td><%= note.title %></td><td><%= User.find(note.user_id).name %></td></tr>
    <% end %>
  </table>
</div>
